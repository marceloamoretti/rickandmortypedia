import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1 },
  imageBackground: {
    position: 'absolute',
    top: 0,
    height: 200,
    width: '100%',
  },
});

export default styles;
