import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center', marginTop: 150 },
  image: { width: 200, height: 200, marginBottom: 20 },
});

export default styles;
